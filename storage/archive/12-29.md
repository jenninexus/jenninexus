# JenniNexus Action Plan - December 29, 2025

## 🎯 Session Goals
- [x] Analyze and integrate UI/UX enhancements from reference attachments
- [x] Standardize playlist rendering across all pages
- [x] Implement modern UI features (3D tilt, scroll progress, parallax)
- [x] Improve RSS feed reliability with proxy fallbacks

## 🚀 Proposed Enhancements (from Reference)

### 1. UI/UX Standardization
- **Playlist Row Pattern**: Standardize `news.php`, `tankoff.php`, and `playlists.php` to use:
    - `.glass-panel.p-4.text-center` for section headers.
    - `.playlist-grid.row.g-4` for consistent spacing.
    - 16:9 aspect ratio enforcement via `.playlist-thumbnail`.
- **Glassmorphism**: Adopt the "Dark Glass" standard sitewide:
    - `background: rgba(0, 0, 0, 0.6) !important;`
    - `backdrop-filter: blur(10px);`
    - `border: 1px solid rgba(255, 107, 0, 0.3) !important;`

### 2. Modern Interactive Features
- **3D Card Tilt**: Add subtle ±8° perspective tilt to `.playlist-card` and `.game-card` (Desktop only).
- **Scroll Progress Bar**: Add a linear-gradient progress bar at the top of long pages (`blog.php`, `gamedev.php`).
- **Animated Stat Counters**: Animate social follower counts on `links.php` from 0 to target values.
- **Parallax Hero**: Add 0.15x parallax movement to hero titles on `index.php` and `gamedev.php`.
- **Toast Notifications**: Implement `mgShowToast()` for copy-to-clipboard feedback on `links.php`.

### 3. Technical Improvements
- **RSS Proxy Fallbacks**: Update `youtube-grid.js` to use the 3-proxy fallback system from `storage/refrence/playlist-loader.js` (AllOrigins → CorsProxy.io → CodeTabs) when the server-side `api/get-youtube.php` fails.
- **Single Source of Truth**: Consolidate key color variables into `public_html/resources/css/all-themes.css` (add `--jenni-primary`, `--jenni-secondary`, `--glass-bg`, `--color-primary-rgb`) and replace hard-coded hex values where practical.

### Reference Features to Apply (mapped)
- From `storage/refrence/_mixins.scss` → Add compiled utilities in `custom.css`: `.glass-panel`, `.hover-lift`, `.focus-ring`, `.reduced-motion` wrappers, `.line-clamp` helpers, `.stat-number` styling.
- From `storage/refrence/_playlists.scss` → Standardize playlists to `.playlist-grid.row.g-4`, use `.playlist-thumbnail` (aspect-ratio:16/9), and adopt hover/overlay patterns (`.playlist-play-icon`).
- From `storage/refrence/gaming-ui.js` → Extract: `initializeStatCounters()` (animated counters), `initializeParallax()` (0.15x hero parallax), `initializeCardTilt()` (desktop 3D tilt), `initializeScrollProgress()` and `mgShowToast()`.
- From `storage/refrence/playlist-loader.js` → Use CORS proxy list and XML parsing strategy as a robust fallback for RSS feed fetching when server-side proxies fail.

These will be implemented in the order: 1) stat counters + UI script, 2) CSS utilities, 3) youtube-grid proxy fallback, 4) parallax/tilt/scroll progress, 5) theme variable audit.

## 📝 Tasks
- [x] Create `src/assets/js/ui-effects.js` (stat counters, tilt, parallax, scroll progress, smooth scroll, toasts).
- [x] Add compiled CSS utilities (glass-panel, hover-lift, focus-ring, reduced-motion) to `src/assets/css/custom.css`.
- [x] Refactor `public_html/resources/js/youtube-grid.js` to include `fetchPlaylistViaProxies()` fallback.
- [x] Implement 5-column grid support (`.col-lg-2-4`) in `custom.css` and `youtube-grid.js`.
- [x] Update `catgame.php`, `botborgs.php`, `purgatoryfell.php`, and `jennistyles.php` to 5-column layouts and glass hero sections.
- [x] Add `ui-effects.js` include to `includes/footer.php`.
- [x] Update `media.php` to render counts with `data-target` and use `ui-effects.js` for animated stat counters.
- [x] Standardize hero sections with `glass-panel` and `hero-title` across `index.php`, `gaming.php`, `gamedev.php`, `diy.php`, `music.php`, `blog.php`, `youtube.php`, `links.php`, and `media.php`.
- [x] Update all YAML playlist configs (`gaming.yaml`, `gamedev.yaml`, `diy.yaml`, `music.yaml`, `ai.yaml`, `live.yaml`, `youtube.yaml`, `gamejams.yaml`) to use 5-column layouts (`lg: 5`).
- [x] Audit `public_html/resources/css/all-themes.css` for hard-coded hex colors and map key variables to `--jenni-primary`, `--jenni-secondary`, `--glass-bg`, `--color-primary-rgb` as needed.
- [x] **Neon Consistency Audit**: Centralized glow effects in `all-themes.css` using `--glow-*` variables.
- [x] **Navigation Redesign**: Implemented glassmorphism, 3D tilt, and scroll progress in `nav.php` and `offcanvas.php`.
- [x] **Iconography Migration**: Standardized on FontAwesome 6.7.2 across all main pages and game sub-pages.
- [x] **Social Links Refinement**: Unified social link styling via `.social-links-badass`.
- [x] **Deploy Script Audit**: Ensure `deploy.ps1` handles SVG purging and asset synchronization correctly.
- [x] **Final Verification**: Run build and test site navigation.
- [x] Implement `ui-effects.js` (tilt, parallax, counters, scroll progress, scroll-to-top).
- [x] Accessibility audit and keyboard support for UI effects.
- [x] Consolidate JS (integrated back-to-top into ui-effects).
- [x] Aesthetic refinement (neon glow, hover states).
- [x] Neon Consistency Audit: Replaced hard-coded RGBA values in `all-themes.css` and `custom.css` with centralized `--glow-*` variables.
- [x] Reference Mixins Update: Synchronized `custom.css` with `_mixins.scss` variable-based architecture (hover-lift, glow effects).
- [x] Verify `prefers-reduced-motion` behavior for all new animations (parallax, tilt, counters).

## 📓 Notes
- All JS should respect `prefers-reduced-motion`.
- Maintain "Isolated Architecture" - no external dependencies.
- Use `RES_ROOT` for all asset paths.
