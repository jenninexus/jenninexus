
# Daily Runbook — 2025-10-24

## Progress Summary (automated update)
- Restored footer icons to the original Bootstrap Icon markup to remove regressions.
- Improved hero social icon centering, increased button size and hover glow for better legibility.
- Updated tag cloud so badges toggle client-side filtering on pages that contain `data-tags` and also include a small link to the full tag page (preserves both behaviors).
 - Fixed include path issues on several blog pages (switched some per-page includes to use `__DIR__`), resolving head/footer missing warnings when served via the router.
 - Created a small markdown article for the AI tools post and wired the featured playlist container to use `RES_ROOT` image paths.
 - Added DIY tag offcanvas and wired `js-yaml` + `youtube-grid.js` to the DIY page so playlists render and the tag UI can populate.
 - Added `js-yaml` + `youtube-grid.js` to `gaming.php` so gaming playlists parse YAML client-side and render correctly.

Purpose: record priorities and immediate action plan for today. Focus on completing tag-cloud/tag-system wiring, verifying include fixes, and tracing the `resumeId` client-side error.

Top priorities (today)
- Ensure tag-cloud and tag-system share a single filtering implementation and UI is available site-wide (offcanvas + active filters). Verify that `data-tags` is present on all major content sections.
- Add/verify `data-tags` on remaining high-traffic pages (music, patreon, live, blog) so the tag cloud can filter them.
- Confirm `jennistyles.php` and other subpage includes are robust (use `__DIR__` includes) and no PHP include warnings remain.
- Investigate and fix the client-side error referencing `resumeId` in built bundles (identify the served `contents.*.js` file, its source, and either initialize `resumeId` safely or patch the build output/source).
- Create site-wide tag index (`/tags.php`) and per-tag pages (`/tag/{slug}`) so tag aggregation and related-tags are server-powered.
- UX polish: ensure multi-part logos (DIY / w/ / Jenni / NEXUS) line-wrap gracefully, and ensure buttons icons/text are vertically centered across pages.

Action items (step-by-step)
1. Validation & smoke test (quick):
	- Start the dev server and open the home page.
	- Open DevTools Console & Network → reproduce the `resumeId` error. Note the filename and URL for the offending bundle (e.g., `contents.04ff201a.js`).
2. Tag system final wiring:
	- Centralize applyFilters logic (move to shared helper or export from `tag-cloud.js`) so both tag-cloud and tag-system call the same function.
	- Confirm `tag-cloud.js` badges have `data-tag-id` and `data-tag-slug` and that `tag-system.js` uses slugs consistently.
	- Add `data-tags` attributes to the remaining content sections (music, blog posts previews, patreon listings, live page highlights).
3. ResumeId error trace & fix:
	- Locate the served `contents.*.js` asset (network tab or server build directory).
	- If it's a built bundle, find its source module and guard the `resumeId` usage or ensure the variable is initialized by `compat-resume.js` (already added as a guard).
	- If the issue persists at runtime, add targeted guards in the bundle or shim the missing API before bundle execution.
4. Site-wide tag pages:
	- `tags.php` (index) — list tags and counts (done).
	- `/tag/{slug}` — aggregate content by tag (done). Verify content sources and URLs are accurate.
5. QA and deploy:
	- Run through home page, DIY, GameDev, Links, Blog pages to confirm tag filtering and UI consistency.
	- Confirm no PHP warnings in subpages.

Notes / decisions made
- `compat-resume.js` added to avoid ReferenceError for `resumeId` while we trace the bundle origin. This is a safe guard; goal is to remove it after the root cause is fixed in the bundle source.
- Tag system UI (offcanvas) added to `index.php` and `tag-cloud.js` is now loaded site-wide so tags show up on pages that include the footer.
- `tags.php` and `/tag/index.php` were created to provide server-side tag aggregation and related-tags by co-occurrence.

Next immediate steps ( I'll run these now )
1) Search the workspace for any `contents.*.js` files to find the built asset.
2) Start the dev server and inspect the terminal output for warnings; then check the dev server's served asset list (if any) and report the network path to the bundle that references `resumeId`.
3) Finish site-wide tag coverage: add `data-tags` attributes to `music.php`, `patreon.php`, and `live.php` where relevant so the tag offcanvas can filter those sections.
4) Consider switching the blog listing to auto-generate (scan `public_html/blog/*.php`) — if we do this, ensure the generated listing includes `data-tags` for each post so `tag-system.js` continues to work (use a slug→tags mapping or parse front-matter).
5) Run a headless browser smoke test (optional) to capture console logs (resumeId) and verify playlists render in Chrome/Chromium. I can run this if you want; it needs puppeteer installed.

