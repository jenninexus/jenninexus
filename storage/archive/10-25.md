# Daily Runbook — 2025-10-25

## Summary
This runbook captures next actions from `10-24.md` and the `TAG-SYSTEM.md` status doc, verifies current gamedev links, and lists prioritized tasks to finish tag-system UI, sitemap polish, and playlist linking.

## What I checked
- Reviewed `storage/10-24.md` (progress and next steps).
- Reviewed `storage/docs/TAG-SYSTEM.md` (tag-system status).
- Inspected `public_html/gamedev.php` to confirm gameplay cards and playlist containers are present and that some game cards already link to internal `/game/*.php` pages.
- Updated `public_html/sitemap.php` to render a human-friendly card grid while keeping the existing XML output.

## Notes / Observations
- `gamedev.php` already contains game cards for Jenni Styles, Cat-As-Trophy, Game Jam Highlights (links to `/game/gamejams.php`), and Purgatory Fell VR (links to `/game/purgatoryfell.php`), but the playlist containers are populated client-side by `resources/js/youtube-grid.js`. If the grid-rendered playlist cards need an "Open Page" button, the data source (YAML) must include a link or `page` field.
- I searched for `resources/playlists/gamedev.yaml` and did not find it in the repo; the `youtube-grid.js` may be pulling data from a different path or remote endpoint. We need to locate or create `resources/playlists/gamedev.yaml` and ensure entries have a `page` or `link` property.
- `music.php` uses a very attractive multi-colored (rainbow) badge/tag style for genre tags. We should keep a note of this palette for global/UX reference (see palette note below).

Palette note (music tags):
- The music page uses distinct Bootstrap color badges (primary, secondary, success, info, warning, danger) producing a pleasant rainbow grid. Recommendation: reuse this palette when we design global tag UI or tag-cloud badge variants so tags are colorful and easily scannable.

## Action plan / TODOs (prioritized)
1. Update sitemap display (DONE)
   - Status: completed. `public_html/sitemap.php` now renders page cards and still supports `?format=xml`.
   - Verification: visit `/sitemap.php` to confirm card grid; visit `/sitemap.php?format=xml` to confirm XML output.

2. Locate or create `resources/playlists/gamedev.yaml` (HIGH)
   - Why: `youtube-grid.js` populates playlist cards from a YAML. The YAML must include `title`, `youtubePlaylistId`, optional `tags`, and ideally a `page` or `link` field for internal routing.
   - Acceptance: file exists and includes entries for:
     - Jenni Styles Dress Up Game -> `/game/jennistyles.php`
     - Cat-As-Trophy -> `/game/catgame.php`
     - Game Jam Highlights -> `/game/gamejams.php`
     - Purgatory Fell VR -> `/game/purgatoryfell.php`
   - Status: not-started.

3. Update `resources/js/youtube-grid.js` to attach internal-page links to playlist cards when YAML includes `page` or `link` (HIGH)
   - Why: clicking playlist cards should open the internal game page or an "Open Page" button should be visible.
   - Acceptance: rendered playlist cards include either an overlay link or a dedicated button that points to the page from YAML.
   - Status: not-started.

4. Create or expose `tags.php` (MEDIUM)
   - Why: `TAG-SYSTEM.md` references a server-side tag index but I couldn't find `tags.php`.
   - Acceptance: `public_html/tags.php` lists tags and links to `/tag/{slug}` (or a single server-side view that shows tag results). The tag offcanvas should link to this index.
      - Status: completed (robust tags.php added; supports ?filters=tag1,tag2 and ingests blog-posts.yaml for nicer titles).

5. Add tag UI/offcanvas to `index.php` and ensure `tag-system.js` is wired site-wide (MEDIUM)
   - Why: Index page currently loads the script but lacks the offcanvas and active filter area in many pages.
   - Acceptance: Offcanvas opens, shows categories, Apply/Clear update displayed `#activeFilters` and run `filterContent()`.
      - Status: in-progress (offcanvas added to index, gamedev, gamejams, music, diy pages; tag-system.js wired on those pages).

6. Ensure content items have `data-tags` where appropriate (music, patreon, live, blog excerpts) (MEDIUM)
   - Why: tag filtering requires `.content-item[data-tags]`.
   - Acceptance: key cards across music, blog index, patreon, live include `data-tags` with comma-separated slugs.
      - Status: in-progress (many high-value cards now include `data-tags`; further scan needed for full coverage).

   6b. Blog index: ensure `blog.php` renders an index of posts with working tag metadata (HIGH)
      - Why: Users need a browsable blog index with tag-based filtering and correct preview cards.
      - Acceptance: `public_html/blog.php` loads `public_html/resources/playlists/blog-posts.yaml`, renders cards with `data-tags` and tag badges, and the top filter buttons use canonical tag slugs (e.g., `voice-acting`). Blog posts should link to `/blog/{slug}.php` where files exist in `/public_html/blog/`.
         - Status: completed (blog thumbnails now resolve via RES_ROOT, badges link to tag pages, top filters wired).

7. Verify gamedev cards & playlist links (HIGH)
   - Steps:
     - Confirm that the four playlists/cards open internal pages.
     - If playlists are rendered by JS, update data source and `youtube-grid.js` to include `page` links.
      - Status: not-started.

8. QA & Smoke test (LOW)
   - Start the dev server, open main pages (index, gamedev, music, links, sitemap), reproduce any console errors (e.g., `resumeId`), and confirm playlist cards and tag filtering behave as expected.
      - Status: not-started.

   ## Recent fixes (done during this pass)
   - `public_html/tags.php` now resolves human-friendly tag names (reads `tags.json`) and displays readable titles/URLs for content entries.
   - Clicking tag content entries that point to paths (e.g., `/diy`, `/media/discord`) now links directly to those paths instead of `/content/...` so they no longer 404.
   - Blog post metadata from `resources/playlists/blog-posts.yaml` is incorporated into the tag matching so blog posts appear with nice titles/links in filtered results.

   ## Tag-system checks performed
   - Removed the large tag cloud from `links.php` so the big visual cloud only appears on the dedicated Tags page.
   - Hardened `public_html/tag/index.php` to support multiple `content_tags.json` shapes (sequential arrays of IDs, or associative objects with `tags`, `title`, `url`). This fixes the "Array to string conversion" warnings and provides safer title/url fallbacks.
   - Updated `public_html/blog.php` to ensure tag slug consistency and cleaner badge rendering.

   ## Next verification steps
   - Start dev server and test these pages:
      - `/blog.php` — verify posts load and tag filters work; check exported `data-tags` on cards.
      - `/tag/index.php?slug=tiktok` — confirm no warnings and that results show.
      - `/links.php` — confirm tag cloud removed and page still functions.
      - `/tags.php` — ensure tag index still lists tags with counts.

   If all good, I'll follow up by wiring offcanvas tag UI into `index.php` and other pages, and by adding `data-tags` to any remaining high-value content pages.

## Immediate next steps I'll take (if you want me to continue now)
- Search the repo for `youtube-grid.js` and inspect where it loads playlist YAML (if present) and how it maps `tags` and `page` into the DOM. If the YAML is missing, propose a minimal `resources/playlists/gamedev.yaml` fragment and patch `youtube-grid.js` to read and render a page button.
- If you want, I can implement the minimal YAML and modify `youtube-grid.js` so the four target playlists have working "Open Page" links.

## Files changed/created in this pass
- Updated: `public_html/sitemap.php` — now renders cards in HTML mode.
- Created: `storage/10-25.md` (this file).

## Closing note
I didn't find `resources/playlists/gamedev.yaml` or a `tags.php` in the workspace. Creating these (or wiring `youtube-grid.js` to the right data path) is necessary for the playlist->internal-page linking to work. Tell me if you'd like me to go ahead and (a) create a minimal `gamedev.yaml` with the four playlists and (b) update `resources/js/youtube-grid.js` to render an "Open Page" button from `page`/`link` fields — I can implement and test that next.

---

## Edits done so far (2025-10-26)

- Replaced PayPal placeholder with `https://paypal.me/jenninexus` and added a PayPal entry to `public_html/resources/playlists/social-stats.json` so the PayPal link can be used site-wide.

## Session update — 2025-10-27

- Reviewed repository layout and confirmed the following:
   - `public_html/sitemap.php` was updated to provide an HTML card-grid view and still emits XML when requested (`?format=xml`). This change is included in the earlier edits noted above.
   - `resources/playlists/gamedev.yaml` was not present in the repo — the site relies on a YAML for `youtube-grid.js` to render playlist cards client-side.
   - `tags.php` was not found during inspection; tag-system appears to be partly client-side and partly server-side (tags JSON exists under `resources/playlists/tags.json`).

- Completed in this session:
   1. Confirmed and documented the missing `gamedev.yaml` and missing `tags.php` (recorded in the runbook above).
 2. Prepared an action plan and created `storage/10-26.md` (new file) that contains a prioritized todo list and step-by-step recommendations to finish the tag-system, playlist linking, and blog thumbnail fixes.

- Next immediate technical steps to take (recommended):
   - Create a minimal `resources/playlists/gamedev.yaml` containing the four key playlists (Devlogs, Jenni Styles, Cat-As-Trophy, Purgatory Fell) and add `page` or `link` fields to each entry so playlist cards can link to internal game pages.
   - Update `resources/js/youtube-grid.js` to detect `page`/`link` and add an "Open Page" CTA to playlist cards. Also ensure playlist cards have `data-tags` attributes that map to tag slugs from `tags.json`.
   - Update `public_html/blog.php` to load thumbnail images via `RES_ROOT` and normalize paths in `resources/playlists/blog-posts.yaml` to point to `/resources/images/blog/...` (or equivalent) so thumbnails load from `public_html/resources`.

If you'd like I can proceed and implement the minimal `gamedev.yaml` and the `youtube-grid.js` changes next; say "please proceed" and I'll patch those files and run quick smoke checks.
- Began tagging sweep: added `data-tags` to several high-value items on `index.php` (category tiles and their `video-grid` containers), and to the Featured Projects links (Purgatory Fell, Botborgs, Jenni Styles, Game Jams).
- Added `data-tags` attributes to several playlist container elements in `public_html/gamedev.php` so the tag-filter UI can target them.
- Created `storage/docs/PATREON.md` documenting the Patreon proxy (`get-patreon-tiers.php`), the secrets location (`storage/secrets/patreon.json`), and CLI testing instructions.
 - Fixed Jenni Styles page: updated `public_html/game/jennistyles.php` to use `<?= RES_ROOT ?>` for the hero screenshot and to render the playlist directly via `YouTubeGrid.renderPlaylist(...)` so the development videos appear reliably on the page.
 - Fixed Jenni Styles page: updated `public_html/game/jennistyles.php` to use `<?= RES_ROOT ?>` for the hero screenshot and to render the playlist directly via `YouTubeGrid.renderPlaylist(...)` so the development videos appear reliably on the page.
 - Fixed Cat-As-Trophy page: updated `public_html/game/catgame.php` to render its playlist directly and removed the redundant "Open Page" CTA from both game pages (playlists now render thumbnails directly on the game page). Both pages now prefer `YouTubeGrid.renderPlaylist()` and fall back to `youtubeLoader.renderPlaylistGrid()` if needed.

## Immediate next steps I'll take if you want me to continue

1. Attempt a local run of `public_html/resources/api/get-patreon-tiers.php` via PHP CLI to detect whether the secrets file exists and whether tiers can be fetched.
2. Continue the `data-tags` sweep across `blog.php` excerpts, `patreon.php` VIP cards, and any remaining high-value playlist grids.
3. If you'd like, create a minimal `resources/playlists/gamedev.yaml` and patch `resources/js/youtube-grid.js` to render an "Open Page" button from `page`/`link` fields.

I'll run the Patreon tiers test next (it may return `no_token` if secrets are not present). I will not start the dev server or run smoke-tests until you confirm you'd like me to do that.
