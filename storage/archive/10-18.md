# JenniNexus - Action Plan & Changes (2025-10-18)

Today (high-priority): Fix Patreon OAuth redirect error and verify login flow
-----------------------------------------------------------------------

Action note (2025-10-19): SSH lockout diagnosis & concise recommendation
----------------------------------------------------------------------
- Summary: Your workstation successfully connects to the server but every offered ED25519 key is rejected by the SSH server; this indicates the server does not have the matching public keys in `/home/deploy/.ssh/authorized_keys` or the SSH-related file permissions/ownership prevent key-based auth. The WinSCP "Error 4" seen earlier also points to a server-side write or /tmp problem (e.g., full /tmp or SFTP subsystem issue) that can block uploads.
- High-level recommended fix: Ensure the `deploy` account's `/home/deploy/.ssh/authorized_keys` contains your public keys (the two keys you keep locally), set `/home/deploy/.ssh` to mode 700 and `authorized_keys` to mode 600 with owner `deploy:deploy`, free /tmp if it's full, and reload sshd. If you cannot run those steps yourself, ask your provider/administrator to run them (this note intentionally omits long command lists per request).


1) Reproduce the error locally / on staging
	- Visit https://jenninexus.com/patreon, click Connect, and capture the exact error message from Patreon (you already saw the redirect URI mismatch).
	- Open DevTools → Network and follow the 302/redirect to capture the authorize URL and confirm the `redirect_uri` parameter value.

2) Confirm server secrets & Patreon app config
	- On the server, ensure `/var/www/jenninexus/storage/secrets/patreon.json` contains the correct `PATREON_REDIRECT_URI` value: `https://jenninexus.com/patreon-callback.php`.
	- Log into Patreon developer settings and add that exact URL to the allowed redirect URIs for your OAuth client. Use the exact scheme+host+path.

3) Quick dev fallback & verification (already applied)
	- Wired the Connect button so it explicitly navigates to `/patreon-auth-start.php` (avoids RES_ROOT or base href path issues).
	- After Patreon app update, click Connect and complete the OAuth flow.

4) If token exchange fails or tokens are not written
	- Check server logs (php/fpm/nginx) and the helper deploy log: `/var/log/jenninexus-deploy.log`.
	- Verify the OAuth callback `patreon-callback.php` can write tokens to `/var/www/jenninexus/storage/patreon/tokens.json` and that the directory is writable by `www-data`.

5) Follow-ups (after OAuth works)
	- Implement small debug mode in `patreon-auth-start.php` to print the authorize URL when `?debug=1` is present (safe for dev only).
	- Ensure webhook secret `PATREON_WEBHOOK_SECRET` is set on the server and `public_html/resources/api/patreon-webhook.php` verifies payloads.
	- Remove any dev-only secrets from repo-backed files and ensure production only uses `/var/www/jenninexus/storage/secrets/patreon.json`.

Notes and assumptions
---------------------
- Assumed Patreon app exists and you have access to edit its redirect URIs.
- Assumed production site is served from `https://jenninexus.com` (no www) and that the registered redirect should match that host exactly.

Next immediate action for me
---------------------------
- If you want, I can add an optional `?debug=1` behavior to `patreon-auth-start.php` (server-only) to print the exact authorize URL for quick verification. Say "yes, add debug" and I'll implement it and run a fast syntax check.

End of 2025-10-18 action plan.
