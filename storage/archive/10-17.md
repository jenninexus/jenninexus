# JenniNexus - Action Plan & Changes (2025-10-17)

What I ran / verified
----------------------
Confirmed `scripts/fix-permissions-jenninexus-remote.sh` exists and contains safe idempotent commands:
 - chown www-data:www-data -R
 - chmod directories 755 / files 644
 - set writable locations (775) for storage & public_html/resources
 - remove legacy `public_html/shared` and `vendor` symlinks if present; this project is self-contained and does not require shared symlinks
 - prints verification via `ls -la`
What I ran / verified
----------------------
Confirmed `scripts/fix-permissions-jenninexus-remote.sh` exists and contains safe idempotent commands:
 - chown www-data:www-data -R
 - chmod directories 755 / files 644
 - set writable locations (775) for storage & public_html/resources
 - remove legacy `public_html/shared` and `vendor` symlinks if present (project is self-contained)
 - prints verification via `ls -la`
# JenniNexus - Action Plan & Changes (2025-10-17)

Summary
-------
- Focus: Make JenniNexus Nginx-friendly, remove Apache/.htaccess from deploy flow, include only necessary maintenance script in deploy package, and verify remote permissions.

Files changed
-------------
- `DEPLOYMENT-MANIFEST.md` — Removed `.htaccess`/Apache references, updated include/exclude lists to be Nginx-friendly, and ensured `scripts/fix-permissions-jenninexus-remote.sh` is listed as an intentionally included maintenance helper.
- `scripts/deploy.ps1` — Removed `.htaccess` creation, skipped Apache artifacts, and added copying of the single maintenance script `fix-permissions-jenninexus-remote.sh` into the top-level `deploy/` folder.
- `scripts/fix-permissions-jenninexus-remote.sh` — Present in `scripts/` (made/validated). This is the only script intentionally included in deploy for remote maintenance.

What I ran / verified
----------------------
- Confirmed `scripts/fix-permissions-jenninexus-remote.sh` exists and contains safe idempotent commands:
  - chown www-data:www-data -R
  - chmod directories 755 / files 644
  - set writable locations (775) for storage & public_html/resources
  - recreate `public_html/shared` and `vendor` symlinks to `/var/www/shared` and `/var/www/shared-deps/vendor` respectively
  - prints verification via `ls -la`

- Note: I previously executed a similar script on the server to fix permissions and symlinks successfully. That run recreated symlinks and showed owned files by `www-data`.

Immediate verification steps (recommended)
-----------------------------------------
1. Visit https://jenninexus.com and verify the site no longer returns 403. Check a few pages (index, gamedev, music).
2. SSH checks (run on server):
   - ls -la /var/www/jenninexus/public_html
   - sudo tail -n 200 /var/log/nginx/jenninexus.error.log
   - sudo systemctl status php8.3-fpm

Deploy notes
------------
- The deploy package should NOT include build scripts or local .ps1 PowerShell files. Only the maintenance helper script is included intentionally.
- `.htaccess` is ignored by Nginx — this repo will no longer create or include `.htaccess` during the deploy step.

Next actions
------------
1. If the site still shows 403, run the maintenance script on the server (it's in `deploy/` after you run `deploy.ps1`) manually: 
   - ssh root@64.23.141.41 "bash -s" < ./deploy/fix-permissions-jenninexus-remote.sh
2. If nginx still denies access, check the server block root and ensure `root /var/www/jenninexus/public_html;` is correct. Reload nginx: `sudo systemctl reload nginx`.
3. Optional: tighten permissions after confirming site works — change to 750/640 for directories/files and update PHP-FPM/nginx user if needed.

Patreon secrets & endpoints (action items)
-----------------------------------------
- Place real Patreon secrets on the server at `storage/secrets/patreon.json` (use `patreon.json.example` as a template). This file MUST NOT be committed to git.
- Suggested callback URL to register on Patreon: `https://jenninexus.com/patreon-callback.php` (route already scaffolded).
- After adding secrets, remove the development fallback at `public_html/resources/playlists/secrets.json` or ensure it contains non-sensitive development-only values.

Remaining tasks
---------------
- Finish wiring `patreon-callback.php` to perform the real OAuth token exchange (requires PATREON_CLIENT_ID/SECRET).
- Implement server-side token caching/refreshing (suggest: `storage/patreon/` using `StorageConfig::getCacheDir('patreon')`).
- (Optional) Implement Patreon webhook receiver to keep membership state in sync.

Changelog (this session)
-------------------------
- 2025-10-17: Removed `.htaccess` creation from `scripts/deploy.ps1`.
- 2025-10-17: Updated `DEPLOYMENT-MANIFEST.md` to be Nginx-centric and to explicitly include only the maintenance script from `scripts/`.
- 2025-10-17: Validated `scripts/fix-permissions-jenninexus-remote.sh` content.

Recent edits (2025-10-18)
-------------------------
- Updated `public_html/game/martiangames.php`:
   - Removed the two circular profile images from the hero section.
   - Removed the GameJolt hero button (kept Steam/YouTube/Patreon links).
   - Replaced author text "Alice Ruppert" with "Shade Muse" and linked the name to https://www.linkedin.com/in/martiangames/.
   - Updated the Tank Off CTA to use the canonical GameJolt URL `https://gamejolt.com/games/tankoff/526957`.
   - Converted Tank Off gallery and modal image references to absolute `/resources/images/gamedev/tankoff/...` paths and updated filenames to match the provided assets (box art, TankOff.png and the 8 screenshots).

- Updated other game pages to link consistently to `/game/martiangames` instead of relative `martiangames.php`:
   - `public_html/game/purgatoryfell.php`
   - `public_html/game/soccercow.php`

Outstanding game-page tasks
--------------------------
- Verify `/game/tankoff` page exists (if a dedicated tankoff page is desired) or ensure the Tank Off modal and gallery load correctly from the Martian Games page.
- Confirm the listed tankoff images exist on disk under `public_html/resources/images/gamedev/tankoff` and are readable by the web server.
- After nginx routing is addressed, consider adding top-level redirects so `/martiangames` also maps to `/game/martiangames` (optional).

If you want, I can now run `.	ests
un-deploy-sanity.ps1` (if you have such a test) or re-run `deploy.ps1 -BuildOnly` and show you the `deploy/` folder contents to confirm only intended files are present. Otherwise, I can SSH and run a targeted verification of `ls -la` and tail the nginx error log.

— End of 10-17 notes
