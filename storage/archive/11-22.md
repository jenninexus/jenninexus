# JenniNexus Action Plan - November 22, 2025

## 🎯 TODAY'S PRIORITIES

### **PRIORITY 1: Deployment Pipeline Integrity**
**Status**: ✅ **COMPLETED**
- [x] **Verify SVG Safety**: Confirmed `optimize-assets.ps1` only targets `.css` and `.js` files. SVGs are safe.
- [x] **Unify Build Pipeline**: Updated `build-and-deploy.ps1` to call `build-all.ps1` (Stage 0) ensuring assets/tags are fresh.
- [x] **Verify Permission Fixes**: Confirmed `deploy.ps1` automatically triggers `fix-permissions-jenninexus-remote.sh`.

### **PRIORITY 2: Documentation Consistency**
**Status**: ✅ **COMPLETED**
- [x] **Update `scripts/README.md`**: Added link to `DEPLOYMENT-GUIDE.md` and clarified the "Systematic Process".
- [x] **Update `DEPLOYMENT-GUIDE.md`**: Documented the new "One Script" workflow (`build-and-deploy.ps1`).
- [x] **Update `POST-DEPLOY-JENNINEXUS.md`**: Noted that permission fixing is automatic with `deploy.ps1`.

### **PRIORITY 3: Routing & Storage Architecture**
**Status**: ✅ **COMPLETED**
- [x] **Fix Remote Permissions Script**: Corrected `cd` logic in `fix-permissions-jenninexus-remote.sh` to prevent safety aborts.
- [x] **Fix Local Routing**: Updated `router.php` to handle `/blog/` URLs correctly (matching Nginx behavior).
- [x] **Audit Cache Locations**: Verified `get-youtube.php` and `get-patreon-tiers.php` point to project root storage (`/var/www/jenninexus/storage/`).
- [x] **Fix Patreon Proxy**: Updated `get-patreon-tiers.php` to use correct root storage path (`dirname(__DIR__, 3)`).
- [x] **Prevent Storage Upload**: Updated `deploy.ps1` to explicitly remove `public_html/storage` from the deploy package.
- [x] **Clean Remote Storage**: Removed redundant `/var/www/jenninexus/public_html/storage` from production server.
- [x] **Update Documentation**: Clarified storage locations in `YOUTUBE.md`.

### **PRIORITY 4: GitHub Backup Preparation**
**Status**: ✅ **COMPLETED**
- [x] **Create Safe Build Script**: Created `scripts/build-github-package.ps1` to generate a clean, secret-free project export.
- [x] **Audit & Secure**: Scanned project for secrets and updated `.gitignore` to strictly exclude `deploy/`, `storage/patreon/`, and `github-package/`.
- [x] **Refine Exclusions**: Updated build script to remove sensitive docs (`authorized_keys.md`, `POST-DEPLOY-JENNINEXUS.md`) and server manifests.
- [x] **Verify Package**: Confirmed `github-package/` contains only safe source code, docs, and dependency definitions.

### **PRIORITY 5: Tag System & Blog Fixes**
**Status**: ✅ **COMPLETED**
- [x] **Fix Blog Tag Filtering**: Replaced broken `onclick` button handlers with direct `href` links to `tags.php` in all blog posts.
- [x] **Fix Video Grid Tags**: Updated `youtube-grid.js` to generate `<a>` tags instead of `<span>` with `onclick` for video cards.
- [x] **Fix Video Aspect Ratios**: Updated `sora-ai-2025.php` to use 9:16 aspect ratio for vertical videos.
- [x] **Fix AI Page**: Refactored `ai.php` to use `youtube-grid.js` instead of hardcoded iframes.
- [x] **Fix AI Page JS**: Corrected `init` call to `loadPageConfig` in `ai.php` to fix playlist loading.
- [x] **Update Documentation**: Updated `BLOG-PAGES.md`, `GAME-PAGES.md`, and `TAG-SYSTEM.md` to reflect the new link-based tag standard.

---

## 📝 NOTES
- **GitHub Safety**: The new `github-package/` directory is the ONLY folder that should be pushed to GitHub. It is physically separated from the working directory to prevent accidental secret leaks.
- **SVG Safety**: `optimize-assets.ps1` uses PurgeCSS (CSS only) and Terser (JS only). It does not process image files.
- **Pipeline**: `build-and-deploy.ps1` is now the single source of truth for deployments. It runs:
  1. `build-all.ps1` (Sync src->public, generate tags)
  2. `optimize-assets.ps1` (Purge/Minify)
  3. Packaging (Excludes `public_html/storage`)
  4. `deploy.ps1` (Upload + Fix Perms)
- **Storage Architecture**:
  - **Correct**: `/var/www/jenninexus/storage/` (Project Root)
  - **Incorrect**: `/var/www/jenninexus/public_html/storage/` (Web Root - should not exist)
  - **Action**: Deployment script now prevents uploading the incorrect folder.

## 🏁 SESSION SUMMARY
### ✅ Completed Objectives
- [x] Verified SVG optimization safety (no risk found).
- [x] Fixed `build-and-deploy.ps1` to include the build step.
- [x] Updated all deployment documentation to match the code.
- [x] Fixed routing issues for blog and remote permissions.
- [x] Enforced "Storage Outside Webroot" rule in code and deployment scripts.
- [x] **Secured Project for GitHub**: Created `github-package` workflow and updated `.gitignore`.

### 🚧 Ongoing Issues
- [x] Clean up remote storage artifacts (removed `public_html/storage`)
- [x] Investigate "Permission denied" on wrong IP (192.241.155.141)
    - Found in `known_hosts` (legacy entry)
    - Removed from `known_hosts` to prevent confusion
- [x] Verify deployment path for `fix-permissions-jenninexus-remote.sh`
    - Source: `scripts/fix-permissions-jenninexus-remote.sh`
    - Deploy Package: `deploy/jenninexus/scripts/`
    - Remote Execution: `/var/www/jenninexus/scripts/`
    - Confirmed `build-and-deploy.ps1` logic handles this correctly
- [x] Review deployment package contents
    - Structure matches remote (`public_html/`, `scripts/`)
    - Identified minor cleanup items (`resources/archived`, `resources/css/!bak`)
    - Verified `patreon-auth-start.php` (root vs resources) - Root is main, resources is proxy
- [x] Enforce strict exclusions for `archived` and `!bak`
    - Modified `scripts/build.ps1` to clean these up at the end of build
    - Modified `scripts/build-and-deploy.ps1` to use robust relative path matching
    - Updated `DEPLOYMENT-GUIDE.md`, `memory.mdc`, and `mcp_jenni.json`
- [x] **Final GitHub Prep**: Verified `github-package` excludes all secrets, IPs, and sensitive docs.

## 📅 December 8, 2025
- [x] Create `storage/docs/DIY.md` - Mobile App Design Concept based on `diy.php`

