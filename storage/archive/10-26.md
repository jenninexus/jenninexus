# Daily Runbook — 2025-10-26 (Action Plan)

Top todos (please keep these at the top):

RES_ROOT rule: Always reference files inside `public_html/resources/` using the server-side `RES_ROOT` constant (<?= RES_ROOT ?>) and ensure client scripts use `window.RES_ROOT` to build URLs. This prevents nested pages from requesting wrong paths and keeps resources consistent across pages.

 
- create a conservative per-game playlist mapping and add CTAs on pages that have confirmed playlists (in-progress) — see `storage/docs/PLAYLIST-MAPPING.md`
- ensure `public_html/gamedev.php` and `public_html/gaming.php` load the page YAMLs (`gamedev.yaml` / `gaming.yaml`) and render featured playlists (done/verified where applicable)
- ensure playlist card tags are canonicalized and tag-system filters operate site-wide (done: canonical mapping + used-tags event)
- ensure `public_html/blog.php` thumbnails load reliably via `RES_ROOT` (done: `blog-posts.yaml` normalized)
- polish `public_html/sitemap.php` styling to match `public_html/links.php` (pending)
- QA: run a dev-server smoke test and fix remaining console/asset issues (pending)

Summary and goals for today
---------------------------
Goal: produce a clear, actionable plan and minimal patches to finish the tag-system integration and playlist -> page wiring so gamedev playlists and blog thumbnails work reliably across the site. Deliverables:

- Updated runbook (this file) describing step-by-step changes
- Minimal `resources/playlists/gamedev.yaml` with page/link fields (if requested)
- Small `youtube-grid.js` patch to render 'Open Page' CTA from YAML `page`/`link`
- Blog thumbnail fixes (use `RES_ROOT`) and normalization guidance for `blog-posts.yaml`
- Visual polish of `sitemap.php` to match `links.php`

Assumptions
-----------
- The site uses `RES_ROOT` as a runtime variable exposed server-side (`<?= RES_ROOT ?>`) and client-side (`window.RES_ROOT`) in some pages. We'll keep using `RES_ROOT` for resource paths.
- `youtube-grid.js` reads YAML files from `public_html/resources/playlists/*.yaml` (if not, we'll search and patch the script to load the right path).
- Tag slugs are available in `public_html/resources/playlists/tags.json`; if not, we'll generate slugs from tag display names.

Planned steps (detailed)
------------------------
1) Create minimal `resources/playlists/gamedev.yaml` (small, low risk)
   - Purpose: ensure the `gamedev` page has a local data source with `id`, `title`, `page` or `link`, and `tags` fields.
   - Deliverable: YAML with 4-6 playlists (Devlogs, Jenni Styles, Cat-As-Trophy, Purgatory Fell) and `page` values pointing to `/game/jennistyles.php`, `/game/catgame.php`, `/game/purgatoryfell.php`, etc.

2) Patch `resources/js/youtube-grid.js` (small, targeted)
   - Detect `page` or `link` on playlist entries and render a CTA button ("Open Page") on the playlist card that opens the internal page in the same tab (or in a new tab with target if preferred).
   - Ensure playlist card root element includes `data-tags="tag1 tag2"` or a space/comma-separated attribute, and update client-side tag normalization to use `tags.json` slugs where present.
   - Add defensive checks: if `page` is missing, fall back to the playlist's YouTube URL.

2.1) Additional tasks to add for today
   - Update `public_html/gamedev.php` to use the Martian Games panel style used on `public_html/gaming.php` (same font-family: 'Alien League', same layout and social CTA buttons).
   - Update `public_html/gaming.php` to include a visible `#featured-playlists` container (and any necessary placeholders) and call `YouTubeGrid.loadPageConfig('gaming')` so the `gaming.yaml` playlist config is loaded client-side.
   - Ensure `tag-system.js` is initialized on the gaming page and that the index-style Content Categories tag panel (right-side offcanvas/panel) is reused for `gaming.php` and `gamedev.php` so users get the same tag UI everywhere.

Add these to the day's immediate work list and then start by fixing the `/gamedev` pages and the `/gaming` page playlists.

3) Update `public_html/game/*.php` pages (conservative edits)
   - Add a small playlist CTA block at the top of each game page with a 'Watch Playlist' button that links to either the internal playlist page or the corresponding YouTube playlist.
   - Use `playlist-ids.json` to locate playlist IDs when useful.

4) Blog thumbnails and `blog-posts.yaml`
   - Normalize `image` fields in `resources/playlists/blog-posts.yaml` to be relative to `/resources/images/blog/` (e.g., `/resources/images/blog/ai-tools-2025.jpg`).
   - Update `public_html/blog.php` rendering code to use `RES_ROOT` when setting thumbnail `src`. If the blog grid is rendered client-side from YAML, use `window.RES_ROOT` when constructing `<img>` sources.

5) Tag-system and DIY tag links
   - Ensure tag buttons and tag cloud produce URLs like `/tags.php?tag=slug` or `/tags/slug` depending on site conventions.
   - If `tags.php` is missing, create a simple server-side endpoint that reads `resources/playlists/tags.json` and renders tag pages and redirect-friendly tag URLs.

6) Sitemap style polish
   - Mirror `public_html/links.php` card styles (gradients, icon, centered headings) for `public_html/sitemap.php` HTML output.

7) QA and verification
   - Start the dev server (use the workspace task: "🚀 Start JenniNexus Dev Server") and visit pages: `/`, `/gamedev.php`, `/game/*`, `/blog.php`, `/links.php`, `/sitemap.php` (and `/sitemap.php?format=xml`).
   - Verify: playlist cards show 'Open Page' when `page` is present, tags link to tag pages, blog thumbnails load, sitemap card styling matches links page, and no console errors for missing resources.

Edge cases to cover
-------------------
- Missing `tags.json` or incomplete slugs -> fallback to slugifying tag names client-side.
- Missing images -> use a small placeholder image stored in `public_html/resources/images/_placeholder.png`.
- `youtube-grid.js` may currently load remote playlists; if so, patch conservatively to prefer local YAML when present.

Quick verification checklist for each change
------------------------------------------
- For `gamedev.yaml`: open `/gamedev.php` and confirm new playlist cards appear (or at least 'Open Page' buttons for the target playlists).
- For `youtube-grid.js`: check console for no errors and that cards have the CTA.
- For blog thumbnails: check `/blog.php` loads images and the images resolve to `public_html/resources/images/blog/` path.
- For sitemap: visually verify layout/view parity with `/links.php`.

Follow-ups / larger improvements (post-stabilize)
------------------------------------------------
- Add unit smoke tests for the YAML loader (node script or PHP helper) to ensure playlist metadata contains required fields.
- Add an automated page-validation step to CI or as a dev task that checks common pages for 200 responses and missing assets.
- Consider consolidating playlist metadata into a single JSON index for faster client loads.

If you want me to proceed now I can:

1. Create the minimal `resources/playlists/gamedev.yaml` and commit it.
2. Patch `resources/js/youtube-grid.js` to add the 'Open Page' CTA and tag mapping.
3. Normalize `blog-posts.yaml` image paths and patch `public_html/blog.php` to use `RES_ROOT` for thumbnails.

Tell me which of the three (1-3) you'd like me to do now and I will implement them and run quick smoke checks.

---
Created: 2025-10-27


Progress Update — 2025-10-27 (summary)
-------------------------------------
- Added a Per-Game Playlist Mapping section to `storage/docs/PLAYLIST-MAPPING.md` and seeded it with confirmed mappings (purgatoryfell, martiangames). This provides the source-of-truth for safe CTA insertions.
- Implemented canonical tag mapping in `public_html/resources/js/youtube-grid.js` and exposed used tag slugs via `window.__usedTagSlugs` plus the `YouTubeGrid:usedTagsUpdated` event.
- Updated `public_html/resources/js/tag-system.js` to prefer used tags and added a persisted "Show all tags" toggle; also added server-side offcanvas toggles to common pages so the control is visible before JS loads.
- Normalized `public_html/resources/playlists/blog-posts.yaml` image paths so blog thumbnails resolve consistently via `window.RES_ROOT` / `<?= RES_ROOT ?>`.

Remaining immediate tasks (priority order)
1. Create `public_html/resources/playlists/game-playlist-map.json` with confirmed entries only and empty placeholders for others (I can seed this file now).
2. Patch `/game/*.php` pages to render a conservative server-side CTA when a mapping exists (only pages with an explicit mapping will be changed).
3. Polish `public_html/sitemap.php` styling to match `public_html/links.php` visuals.
4. Run dev-server smoke tests and fix any console/asset issues.
5. Verify or create `tags.php` endpoint (ensure tag index is accessible and offcanvas links point correctly).

If you want me to proceed now I will:

A) Create `game-playlist-map.json` seeded with confirmed IDs and placeholders, then
B) Patch pages that have confirmed mappings (conservative approach), and
C) Run quick fetch-based smoke tests to verify the changes.

Tell me whether you want me to apply A → B → C now (I'll proceed), or if you'd rather review the seeded mapping file first.


